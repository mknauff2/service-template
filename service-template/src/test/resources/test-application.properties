# enable the H2 in-memory data database console
spring.h2.console.enabled=true
# print all SQL queries to the console
spring.jpa.show-sql=true
# disable automatice hibernate schema creation to avoid testing issues
spring.jpa.defer-datasource-initialization=true